# 이 파일을 복사해 .env 로 이름을 바꾸고 실제 값을 넣는다 (.env 는 git 에 안 올라감)
# Supabase 화면 → Project Settings → API 에서 찾는다. 열쇠는 "publishable" 로 시작하는 공개용을 쓴다
SUPABASE_URL=https://프로젝트ID.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_여기에_열쇠
